2026-01-18
- Added a full journey simulation test that logs lesson progression and phoneme requirements.
- Fixed lesson progression so lessons with no phonemes are treated as complete.
- Tests run: full-journey-simulation.test.ts, full-curriculum.test.ts.
- Issue documented: empty-phoneme lessons block progression.
- Extended simulator to model wrong answers with penalties and full-journey mode.
- Tests run: FULL_JOURNEY_SIM=1 full-journey-simulation.test.ts (reached max lesson 89).
- Removed hard max lesson cap and adjusted simulator to run by sessions instead of max lesson.
- Tests run: full-journey-simulation.test.ts.
- Added repeat-card fallback so sessions are always filled (no empty lessons).
- Tests run: FULL_JOURNEY_SIM=1 full-journey-simulation.test.ts.
- Enforced 20 cards per session and failed-card reappearance within 3 sessions.
- Prioritized failed cards in review selection; capped SM-2 interval to avoid date overflow.
- Tests run: FULL_JOURNEY_SIM=1 full-journey-simulation.test.ts (passes).

2026-01-19
- Fixed duplicate card ID issue in generate-cards.ts (duplicate 'ram' and 'room' in word lists).
- Added duplicate detection to generateCVCCards() to prevent duplicate entries in generated cards.
- Added fallback to phonemes/sentences when prioritizing words but none available.
- Improved card queue filling to aggressively introduce phonemes from ALL lessons (1-100).
- System now always fills sessions with 20 cards by introducing more phonemes as needed.
- **CRITICAL FIX**: Fixed lesson progression stalling - phonemes are now proactively introduced
  at the start of each session, not just when more cards are needed. Before this fix, children
  could be stuck at level 1 indefinitely even after months of practice.
- Fixed level progression to be exactly 1 level per session (level never exceeds session count).
- Phonemes are introduced to ensure 20 cards per session, but level advancement is gated.
- Simulation shows proper progression: Level 501 after 500 sessions (exactly 1 per session).
- Tests run: all 107 tests pass (15 test files).
Next:
- Validate on-device progression telemetry to confirm real user path advances.
