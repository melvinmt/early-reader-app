# Supabase Configuration
# Get these from: Supabase Dashboard → Settings → API
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# RevenueCat (for subscriptions)
# Get this from: RevenueCat Dashboard → Project Settings → API Keys
EXPO_PUBLIC_REVENUECAT_API_KEY=your-revenuecat-api-key

# Note: The following secrets must be set in Supabase Edge Functions (not here):
# - GEMINI_API_KEY: Set via `supabase secrets set GEMINI_API_KEY=your-key`
